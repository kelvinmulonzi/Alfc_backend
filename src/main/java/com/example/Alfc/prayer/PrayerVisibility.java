package com.example.Alfc.prayer;

public enum PrayerVisibility {
    /** Posted to the public prayer wall (still anonymous). */
    PUBLIC_WALL,
    /** Only the prayer team sees this one. */
    PRAYER_TEAM_ONLY
}
